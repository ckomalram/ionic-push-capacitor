import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starterpushcap',
  appName: 'pushappcapacitor',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
